package hu.bme.mit.sysml2autosar.transformation;


//Talán erre nem lesz már szükség, elég a BatchModelTransformation
public class Transformation {
	/*
	 private final BatchTransformationBuilder transformationBuilder;
	 
	 public Transformation() { //umlresourceSet paraméter

	     ViatraQueryEngine engine = ViatraQueryEngine.on(new EMFScope(umlresourceSet));
	     transformationBuilder = BatchTransformation.forEngine(engine);
	     
	     List<RuleSet> ruleSets = new ArrayList<>();
	        for (RuleSet ruleSet : ruleSets) {
	        	ruleSet.createRules(engine);
	        	for(BatchTransformation rule : ruleSet.createRules(engine)) {
	        		transformationBuilder.addRules(rule);
	        	}
	        }
	 }
	 */


}
